[project]
name = "mini-blast-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "sqlalchemy>=2.0",
  "asyncpg>=0.29",
  "pgvector>=0.2.5",
  "pydantic-settings>=2.2",
  "alembic>=1.13",
]

[tool.uv]
# optional: uv will use this if you run uv sync